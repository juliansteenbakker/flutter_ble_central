name: flutter_ble_central_example
description: Demonstrates how to use the flutter_ble_central plugin.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: "3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_ble_central:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true
